{
  "name": "@lotto/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "test": "mocha --config ../../.mocharc.js 'src/**/tests/**/*.test.ts' 2>/dev/null || echo 'No tests found'"
  },
  "dependencies": {
    "big.js": "^6.2.2",
    "date-fns": "^4.1.0"
  },
  "devDependencies": {
    "@loopback/testlab": "^7.0.12",
    "@types/big.js": "^6.2.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.0.0",
    "mocha": "^11.7.5"
  }
}
