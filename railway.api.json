{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "pnpm install && pnpm build"
  },
  "deploy": {
    "startCommand": "pnpm migrate && node apps/api/dist/index.js",
    "healthcheckPath": "/ping",
    "healthcheckTimeout": 300
  }
}